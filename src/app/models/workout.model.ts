export class WorkOut {
    type: string;
    minutes: number;

    constructor(type: string, minutes: number) {
        {
            this.type = type;
            this.minutes = minutes;
        }
    }

}